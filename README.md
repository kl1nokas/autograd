# autograd
Autograd is a lightweight library for automatic differentiation. It builds dynamic computational graphs to compute gradients using the chain rule (backpropagation). Supporting operations like addition, multiplication, exponentiation, and activation functions, it provides a foundation for understanding how modern frameworks.
